{
  "name": "assetnest",
  "version": "1.0.0",
  "description": "AssetNest — Digital Property Management Solution (monorepo root)",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c cyan,green \"npm run client\" \"npm run server\"",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["assetnest", "property-management", "real-estate", "saas"],
  "author": "",
  "license": "MIT"
}
